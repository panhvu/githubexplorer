query RepositoryQuery ($keyword:String!, $endCursorId:String){
  search(query:$keyword, type:REPOSITORY, last: 20, after:$endCursorId) {
    repositoryCount
    pageInfo {
      hasNextPage
      endCursor
    }
    repositories: nodes {
        ... on Repository {
          name
          description
          forkCount
          owner {
            avatarUrl
          }

          stargazers {
            totalCount
          }
          watchers (last: 20) {
            watchers: nodes {
                name
                avatarUrl
            }
            totalCount
          }
        }
    }
  }
}